name: end-to-end release compatibility

on:
  merge_group:
  push:
  pull_request:

jobs:
  end_to_end_release_compatibility:
    name: Check PR against end-to-end tests
    runs-on: ubuntu-latest

    steps:
      - name: Dispatch E2E tests
        uses: codex-/return-dispatch@v1
        id: return_dispatch
        with:
          owner: hasura
          ref: refs/remotes/origin/i-am-tom/distinct-id
          repo: v3-e2e-testing
          token: ${{ secrets.E2E_WORKFLOW_PAT }}
          workflow: cargo-test-postgres.yaml
          workflow_inputs: '{ "connector": "${{ github.sha }}" }'

      - name: Await results
        uses: codex-/await-remote-run@v1.0.0
        with:
          owner: hasura
          ref: main
          repo: v3-e2e-testing
          run_id: ${{ steps.return_dispatch.outputs.run_id }}
          token: ${{ secrets.E2E_WORKFLOW_PAT }}
